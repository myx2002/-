//现有SysY程序利用
int factorial(int n) {
    if (n == 0)
        return 1;
    return n * factorial(n - 1);
}

int main() {
    int n = 5;
    int result = factorial(n);
    if(result == 0 && result == n)
        write(1);
    else
        write(result);
    return 0;
}